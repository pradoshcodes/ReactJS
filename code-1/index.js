import ReactDOM from 'react-dom'
import App from './App'

let root = ReactDOM.createRoot(document.getElementById('try'))

root.render(<App/>)

//ReactDOM.render("what you want to render", "where you want render")